
<div class="col-sm-12">
    <div class="admin-section-title">
        @if($video)
            <h3>{{ $video->title }}</h3>
        @else
            <h3><i class="fa fa-plus"></i> Add New Video</h3>
        @endif
    </div>
</div>